<%= simple_form_for @denun do |f| %>
    <%= f.input :name, label: "Nombre"  %>
    <%= f.input :description,label: "Descripcion"  %>
    <%= f.input :denun_type_id, collection: @types, label: "Tipo"  %>
    <%= f.button :submit, value: 'Agregar' %>
<% end %>